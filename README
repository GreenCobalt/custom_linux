sudo apt install git bc bison flex libssl-dev make libc6-dev libncurses5-dev dosfstools
sudo apt install crossbuild-essential-arm64 gcc-aarch64-linux-gnu

```make deps
make defconfig
make```
automatically uses all cpu cores (nproc * 1.5)
